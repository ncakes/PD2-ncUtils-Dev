# ncUtils

## Overview

ncUtils is a shared utility library for PAYDAY 2 mods. ncUtils checks its version when loaded and will not replace a newer version already in use. The development setup in this repository can force-load the development version and enable devmode to prevent other mods from loading another copy of ncUtils.
